apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: queue
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: queue
    spec:
      containers:
      - name: queue
        image: rabbitmq:3.7.3-management-alpine
        ports:
        - containerPort: 5672